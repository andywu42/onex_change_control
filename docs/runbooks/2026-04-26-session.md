# Session Runbook — 2026-04-26

> Instance materialized from `docs/runbooks/session-template.md` (OMN-9781) for
> tonight's session. Headline ticket: OMN-9762. Receipts will be written to
> `onex_change_control/drift/dod_receipts/OMN-9762/`.

## Session metadata
- session_id: <uuid-pending-set_session>
- started_at: 2026-04-26T14:51:15Z
- bound_by: foreground-claude
- mode: build

## Headline tickets
- OMN-9762: Phase 2 Task 5 — add normalize_event_bus normalization function for legacy event_bus block stripping
  - source: linear
  - contract_path: onex_change_control/contracts/OMN-9762.yaml

## DoD evidence cache
> Materialized from each headline ticket's contract DoD evidence items and probe commands.
> Per-item `probe_command` verifies the contract's `check_value` (the receipt file)
> directly, since `check_type: file_exists`. The shared CI gate captured as the
> third row is the upstream proof both per-item receipts depend on (sourced from
> the contract's CI requirement). Note: today's contract has the receipt-file
> probe pointing at the receipt itself — flagged in the Per-step executor table
> (verify-on-claim row) as needing stronger proof.

| ticket | item_id | check_type | probe_command | expected_output |
|---|---|---|---|---|
| OMN-9762 | dod-001 | file_exists | test -f drift/dod_receipts/OMN-9762/dod-001/file_exists.yaml | exit 0 (file present) |
| OMN-9762 | dod-002 | file_exists | test -f drift/dod_receipts/OMN-9762/dod-002/file_exists.yaml | exit 0 (file present) |
| OMN-9762 | ci-gate-pr-916 | ci | gh pr checks 916 --repo OmniNode-ai/omnibase_core | every check state == "SUCCESS" |

## Per-step executor table

| step | current_executor | target_executor | tracking_ticket | notes |
|---|---|---|---|---|
| bind headline | manual: foreground reads contract YAML | /onex:set_session |  | Task 13 fills tracking ticket |
| initial probe | manual: foreground runs probe via Bash | /onex:dod_verify |  | Task 13 fills tracking ticket |
| 1hr tick | manual: CronCreate prompt | /onex:session orchestrator |  | Task 13 fills tracking ticket; launchd bundle currently broken |
| verify-on-claim | manual: foreground re-probes | deterministic verify hook |  | Today's contract uses file_exists pointing at receipt — needs stronger proof |
| session end | manual: foreground writes handoff | /onex:session --phase end |  | Task 13 fills tracking ticket |

## Tick log
> One row per cron tick. Foreground appends; receipts written to onex_change_control/drift/dod_receipts/.

| tick_at | items_probed | items_pass | items_fail | items_advisory | escalation |
|---|---|---|---|---|---|

## Session-end checklist
- [ ] Every headline ticket's dod_evidence has at least one PASS receipt with verifier ≠ runner
- [ ] Receipts committed to onex_change_control via PR
- [ ] Manual-step count + total-step count recorded in handoff
- [ ] Each manual step has a tracking_ticket linking to the broken-skill follow-up
- [ ] Cron deleted (CronCreate is session-bound; record IDs in this section before exit)

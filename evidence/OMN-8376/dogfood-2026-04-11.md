# OMN-8376 Dogfood Evidence — 2026-04-11T08:15:00Z

**Hook**: `overseer_foreground_block.py` (handler) + `pre_tool_use_overseer_foreground_block.sh` (shell wrapper)
**PR**: https://github.com/OmniNode-ai/omniclaude/pull/1210 (merged d79a17ad)
**Canonical flag location**: `$ONEX_STATE_DIR/overseer-active.flag` → resolved to `/Volumes/PRO-G40/Code/omni_home/.onex_state/overseer-active.flag`

## Flag content

```yaml
contract_path: /Volumes/PRO-G40/Code/omni_home/docs/overseer/dogfood-2026-04-11.yaml
active_phase: dogfood-verification
started_at: 2026-04-11T08:15:00Z
```

## Results (all 6/6 pass)

| # | Scenario | Expected | Actual | Pass |
|---|----------|----------|--------|------|
| A | Edit `$OMNI_HOME/omniclaude/README.md` (flag present) | BLOCK exit 2 | exit=2, block reason surfaces contract_path + active_phase + rm instructions | yes |
| B | Bash `gh pr merge 1210 --auto` (flag present) | BLOCK exit 2 | exit=2, identical block reason | yes |
| C | Read `$OMNI_HOME/omniclaude/README.md` (flag present) | ALLOW exit 0 empty | exit=0, `{}` | yes |
| D | Grep (flag present) | ALLOW exit 0 empty | exit=0, `{}` | yes |
| E | Edit `/tmp/scratch.txt` (flag present, outside OMNI_HOME) | ALLOW exit 0 empty | exit=0, `{}` | yes |
| F | Edit `$OMNI_HOME/omniclaude/README.md` (flag REMOVED) | ALLOW exit 0 empty | exit=0, `{}` | yes |

## Block reason observed

```
Overseer contract /Volumes/PRO-G40/Code/omni_home/docs/overseer/dogfood-2026-04-11.yaml is active on phase dogfood-verification. Foreground tool blocked. Escalate via contract halt condition or kill overseer with: rm /Volumes/PRO-G40/Code/omni_home/.onex_state/overseer-active.flag
```

Matches the spec from the ticket:
> "Overseer contract {path} is active on phase {phase}. Foreground tool blocked. Escalate via contract halt condition or kill overseer with: rm .onex_state/overseer-active.flag"

## Note on activation surface

This evidence invokes `python3 .../overseer_foreground_block.py` directly against stdin JSON. The full PreToolUse shell-hook chain (pre_tool_use_overseer_foreground_block.sh → common.sh → handler) only fires inside live Claude Code sessions after the plugin is reloaded via `claude plugin uninstall onex@omninode-tools && claude plugin install onex@omninode-tools` (or `/onex:redeploy`). The handler logic is the load-bearing part and is verified end-to-end above.

Flag was created AND removed as part of this verification run — no residual state left in `$ONEX_STATE_DIR`.

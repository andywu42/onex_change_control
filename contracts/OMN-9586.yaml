---
schema_version: "1.0.0"
ticket_id: OMN-9586
summary: "Audit constants_event_types.py — add 7 missing full topic string constants (OMN-9152 follow-up)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnibase_core PR #892 CI checks pass (excluding deploy-gate which is expected to fail
      on feature PRs)"
    command: "gh pr checks 892 --repo OmniNode-ai/omnibase_core --json name,state"
  - kind: manual
    description: "constants_event_types.py contains all 7 new TOPIC_* constants added in PR #892"
    command: "gh pr diff 892 --repo OmniNode-ai/omnibase_core"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "7 new TOPIC_* constants added to constants_event_types.py and importable from canonical
      path"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9586/dod-001/file_exists.yaml"
  - id: dod-002
    description: "omnibase_core PR #892 CI checks all pass (Mypy, contract compliance, cross-repo boundary,
      etc.)"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9586/dod-002/file_exists.yaml"

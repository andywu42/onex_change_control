---
schema_version: '1.0.0'
ticket_id: OMN-9622
summary: 'feat(core): add EnumAgentProtocol enum for A2A wire protocol classification'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: >
      18 unit tests covering EnumAgentProtocol values, string coercion, Pydantic integration, optional-field
      usage, and YAML round-trip — all passing.
    command: uv run pytest tests/unit/enums/test_enum_agent_protocol.py -v
  - kind: ci
    description: >
      omnibase_core PR #898 passes all CI gates including Code Quality, mypy, ruff, pre-commit, and all
      40 test splits.
    command: gh pr checks 898 --repo OmniNode-ai/omnibase_core
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: >
      Added src/omnibase_core/enums/enum_agent_protocol.py implementing EnumAgentProtocol(StrValueHelper,
      str, Enum) with A2A member and __all__ export.
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-9622/dod-001/file_exists.yaml
  - id: dod-002
    description: >
      Added tests/unit/enums/test_enum_agent_protocol.py with 18 unit tests — 18/18 passing.
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-9622/dod-002/file_exists.yaml
  - id: dod-003
    description: >
      EnumAgentProtocol exported from src/omnibase_core/enums/__init__.py and added to __all__ so consumers
      can import via `from omnibase_core.enums import EnumAgentProtocol`.
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-9622/dod-003/file_exists.yaml

---
# ModelTicketContract — change-aware test selection (epic OMN-9826)
# Plan: docs/plans/change-aware-ci-omnibase-core.md
# Task 13: Add nightly full-suite schedule + keep push/merge_group full
schema_version: "1.0.0"
ticket_id: OMN-9855
summary: "Nightly full-suite schedule trigger added to ci.yml (cron '13 7 * * *') alongside push/pull_request/merge_group/workflow_dispatch
  triggers; adds tests/unit/scripts/ci/__init__.py + test_detect_test_paths.py with 2 schedule-escalation
  tests (skipped pending OMN-9862 + OMN-9863)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnibase_core PR #926 CI checks pass"
    command: "gh pr checks 926 --repo OmniNode-ai/omnibase_core --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Code change shipped: .github/workflows/ci.yml on: block contains schedule cron '13 7
      * * *' alongside push.branches=[main], pull_request, merge_group, workflow_dispatch; new file tests/unit/scripts/ci/__init__.py
      with SPDX header; new file tests/unit/scripts/ci/test_detect_test_paths.py with 2 schedule-escalation
      tests — receipt at drift/dod_receipts/OMN-9855/dod-001/"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9855/dod-001/file_exists.yaml"
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9855/dod-001/file_exists.yaml &&
          grep -q '^evidence_item_id: dod-001$' drift/dod_receipts/OMN-9855/dod-001/file_exists.yaml &&
          grep -q '^ticket_id: OMN-9855$' drift/dod_receipts/OMN-9855/dod-001/file_exists.yaml && grep
          -q 'pr_number: 926' drift/dod_receipts/OMN-9855/dod-001/file_exists.yaml"
  - id: dod-002
    description: "Two schedule-escalation tests authored in tests/unit/scripts/ci/test_detect_test_paths.py:
      test_schedule_event_forces_full_suite (asserts is_full_suite=True with EnumFullSuiteReason.MAIN_BRANCH)
      and test_schedule_on_non_main_still_escalates (asserts is_full_suite=True with EnumFullSuiteReason.SCHEDULED).
      Tests are @pytest.mark.skip pending OMN-9862 (compute_selection) and OMN-9863 (EnumFullSuiteReason)
      landing — receipt at drift/dod_receipts/OMN-9855/dod-002/"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9855/dod-002/file_exists.yaml"
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9855/dod-002/file_exists.yaml &&
          grep -q '^evidence_item_id: dod-002$' drift/dod_receipts/OMN-9855/dod-002/file_exists.yaml &&
          grep -q 'test_schedule_event_forces_full_suite' drift/dod_receipts/OMN-9855/dod-002/file_exists.yaml
          && grep -q 'test_schedule_on_non_main_still_escalates' drift/dod_receipts/OMN-9855/dod-002/file_exists.yaml"
  - id: dod-003
    description: "Acceptance criteria satisfied: on.schedule.cron == '13 7 * * *', on.merge_group present,
      on.push.branches contains 'main' — receipt at drift/dod_receipts/OMN-9855/dod-003/"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9855/dod-003/file_exists.yaml"
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9855/dod-003/file_exists.yaml &&
          grep -q '^evidence_item_id: dod-003$' drift/dod_receipts/OMN-9855/dod-003/file_exists.yaml &&
          grep -q '13 7 \\* \\* \\*' drift/dod_receipts/OMN-9855/dod-003/file_exists.yaml && grep -q 'merge_group'
          drift/dod_receipts/OMN-9855/dod-003/file_exists.yaml"

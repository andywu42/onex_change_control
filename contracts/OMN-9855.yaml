---
# ModelTicketContract — change-aware test selection (epic OMN-9826)
# Plan: docs/plans/change-aware-ci-omnibase-core.md
schema_version: "1.0.0"
ticket_id: OMN-9855
summary: "Nightly full-suite schedule trigger added to ci.yml (cron 13 7 * * *) plus push/merge_group
  preserved"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnibase_core PR #926 CI checks pass"
    command: "gh pr checks 926 --repo OmniNode-ai/omnibase_core --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "ci.yml workflow has schedule trigger '13 7 * * *' alongside existing push/pull_request/merge_group/workflow_dispatch
      triggers"
    source: generated
    checks:
      - check_type: command
        check_value: "uv run python -c \"import yaml; d = yaml.safe_load(open('.github/workflows/ci.yml'));
          on = d.get(True, d.get('on', {})); print('schedule:', on.get('schedule')); print('triggers:',
          sorted(on.keys()))\""

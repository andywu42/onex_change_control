---
schema_version: "1.0.0"
ticket_id: OMN-9456
summary: "fix(OMN-9456): single-authority wiring for node_registration_orchestrator — defer wire_dispatchers
  to generic contract auto-wiring"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: Unit tests pin four invariants — skipped result, legacy helper not called, dispatch engine
      not touched, idempotency
    command: "uv run pytest tests/unit/nodes/node_registration_orchestrator/test_plugin_wire_dispatchers_deferred.py
      -v"
  - kind: tests
    description: Integration test confirms contract declares event_bus.subscribe_topics + handler_routing
      and plugin passes without touching dispatch engine
    command: "uv run pytest tests/integration/nodes/test_plugin_wire_dispatchers_single_authority.py -v"
  - kind: ci
    description: CI passes on PR branch
    command: "gh pr checks 1378 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: Unit tests pass — wire_dispatchers returns skipped, legacy helper not invoked, engine
      not touched, idempotent
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9456/dod-001/command.yaml"
  - id: dod-002
    description: Integration test passes — contract declares subscribe_topics + handler_routing; plugin
      skips dispatch engine
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9456/dod-002/command.yaml"

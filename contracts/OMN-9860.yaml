---
# ModelTicketContract — change-aware test selection (epic OMN-9826)
# Plan: docs/plans/change-aware-ci-omnibase-core.md
schema_version: "1.0.0"
ticket_id: OMN-9860
summary: "Add module adjacency YAML config (scripts/ci/test_selection_adjacency.yaml)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnibase_core PR #925 CI checks pass (canonical implementation lives in omnibase_core)"
    command: "gh pr checks 925 --repo OmniNode-ai/omnibase_core --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Adjacency YAML parses with schema_version=1, all 38 src/omnibase_core subdirs have adjacency
      entries, shared_modules is non-empty subset of adjacency keys — receipt at drift/dod_receipts/OMN-9860/dod-001/"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9860/dod-001/file_exists.yaml"
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9860/dod-001/file_exists.yaml &&
          grep -q '^evidence_item_id: dod-001$' drift/dod_receipts/OMN-9860/dod-001/file_exists.yaml &&
          grep -q '^ticket_id: OMN-9860$' drift/dod_receipts/OMN-9860/dod-001/file_exists.yaml && grep
          -q '^pr_number: 925$' drift/dod_receipts/OMN-9860/dod-001/file_exists.yaml && grep -q '^exit_code:
          0$' drift/dod_receipts/OMN-9860/dod-001/file_exists.yaml"

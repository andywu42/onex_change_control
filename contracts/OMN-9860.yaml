---
# ModelTicketContract — change-aware test selection (epic OMN-9826)
# Plan: docs/plans/change-aware-ci-omnibase-core.md
schema_version: "1.0.0"
ticket_id: OMN-9860
summary: "Add module adjacency YAML config (scripts/ci/test_selection_adjacency.yaml)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnibase_core PR #925 CI checks pass"
    command: "gh pr checks 925 --repo OmniNode-ai/omnibase_core --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Adjacency YAML parses with schema_version=1, all 38 src/omnibase_core subdirs have adjacency
      entries, shared_modules is non-empty subset of adjacency keys"
    source: generated
    checks:
      - check_type: command
        check_value: "uv run python -c \"import yaml; data = yaml.safe_load(open('scripts/ci/test_selection_adjacency.yaml'));
          print('schema_version=', data.get('schema_version')); print('adjacency_keys=', len(data.get('adjacency',
          {}))); print('shared_modules_count=', len(data.get('shared_modules', [])))\""

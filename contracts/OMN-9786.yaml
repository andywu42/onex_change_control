---
schema_version: "1.0.0"
ticket_id: OMN-9786
summary: "Add adversarial invariants (schema_version, verifier, probe_command, probe_stdout) + ADVISORY/PENDING
  enum members to ModelDodReceipt"
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: ci
    description: "omnibase_core PR #923 CI checks pass"
    command: "gh pr checks 923 --repo OmniNode-ai/omnibase_core --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "ModelDodReceipt exposes the 4 new fields (schema_version, verifier, probe_command, probe_stdout)
      and EnumReceiptStatus exposes {PASS, FAIL, ADVISORY, PENDING}"
    source: generated
    checks:
      - check_type: command
        check_value: "uv run python -c \"from omnibase_core.models.contracts.ticket.model_dod_receipt
          import ModelDodReceipt; from omnibase_core.enums.ticket.enum_receipt_status import EnumReceiptStatus;
          print('schema=', list(ModelDodReceipt.model_fields.keys())); print('enum=', [e.value for e in
          EnumReceiptStatus])\""
  - id: dod-002
    description: "Adversarial invariant tests pass (TestEnumReceiptStatusMembers + TestModelDodReceiptAdversarialInvariants
      — 36 tests covering enum membership, SemVer 2.0.0, PASS-only downgrade, PENDING stdout exemption,
      FAIL/PENDING preservation)"
    source: generated
    checks:
      - check_type: test_passes
        check_value: "uv run pytest tests/unit/models/contracts/test_model_dod_receipt.py::TestEnumReceiptStatusMembers
          tests/unit/models/contracts/test_model_dod_receipt.py::TestModelDodReceiptAdversarialInvariants
          -q"
  - id: dod-003
    description: "Receipt-gate consumer (ModelDodReceipt + receipt_gate.py) and existing runtime_sha_match
      DoD gate tests still pass with the new required fields"
    source: generated
    checks:
      - check_type: test_passes
        check_value: "uv run pytest tests/unit/models/contracts/ tests/unit/scripts/validation/ -q"

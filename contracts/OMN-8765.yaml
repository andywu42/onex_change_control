---
schema_version: '1.0.0'
ticket_id: OMN-8765
summary: 'S18: CI lint gate — deterministic-skill enforcement (SkillRoutingError required) [OMN-8765]'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'AST-based validator + pre-commit wrapper shipped in omnibase_core#888 (cross-repo target;
      paths are relative to the OmniNode-ai/omnibase_core repo, not this repo)'
    command: 'gh api repos/OmniNode-ai/omnibase_core/pulls/888/files --jq ''.[] | select(.filename ==
      "scripts/validate_deterministic_skill_routing.py" or .filename == "scripts/pre_commit_validate_deterministic_skills.sh")
      | .filename'''
  - kind: tests
    description: '18 unit tests in omnibase_core#888 cover AST detection of LLM SDK imports, subprocess
      orchestration, missing dispatch, and prose-fallback paths (cross-repo; test file lives in OmniNode-ai/omnibase_core)'
    command: 'gh api repos/OmniNode-ai/omnibase_core/pulls/888/files --jq ''.[] | select(.filename ==
      "tests/unit/scripts/test_validate_deterministic_skill_routing.py") | .additions'''
  - kind: ci
    description: 'Blocking CI job `check-deterministic-skills` added to omnibase_core#888 .github/workflows/ci.yml
      + pre-commit hook `validate-deterministic-skill-routing` registered in .pre-commit-config.yaml (cross-repo
      target)'
    command: 'gh api repos/OmniNode-ai/omnibase_core/pulls/888/files --jq ''.[] | select(.filename ==
      ".github/workflows/ci.yml" or .filename == ".pre-commit-config.yaml") | .filename'''
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: 'AST-based validator + pre-commit shell wrapper shipped in omnibase_core#888 (cross-repo)'
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-8765/dod-001/file_exists.yaml
  - id: dod-002
    description: '18 unit tests pass in omnibase_core#888 covering AST detection, subprocess orchestration,
      missing dispatch, and prose-fallback paths (cross-repo)'
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-8765/dod-002/file_exists.yaml
  - id: dod-003
    description: 'Blocking CI job `check-deterministic-skills` + pre-commit hook `validate-deterministic-skill-routing`
      registered via omnibase_core#888 for all future Tier 1 skill changes (cross-repo)'
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-8765/dod-003/file_exists.yaml

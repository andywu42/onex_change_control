---
schema_version: "1.0.0"
ticket_id: OMN-9600
summary: "Refresh omnibase_infra documentation: remove stale design artifacts, fix workspace-local .onex_state
  defaults"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "Integration tests confirming AgentRegistry and LocalStubProjectTracker default to workspace-local
      .onex_state (not Path.home())."
    command: "uv run pytest tests/integration/test_omn_9600_path_defaults.py -v"
  - kind: ci
    description: "omnibase_infra PR #1396 carries CI and deploy-gate evidence for OMN-9600."
    command: "gh pr checks 1396 --repo OmniNode-ai/omnibase_infra"
  - kind: manual
    description: "CodeRabbit review threads on PR #1396 addressed with real code fixes."
    command: "gh pr checks 1396 --repo OmniNode-ai/omnibase_infra --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Integration tests for workspace-local .onex_state defaults passed locally."
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9600/dod-001/file_exists.yaml"
  - id: dod-002
    description: "PR #1396 opened with deploy-gate and receipt-gate enforcement."
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9600/dod-002/file_exists.yaml"
  - id: dod-003
    description: "CodeRabbit threads addressed with real code fixes (not dismissed)."
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9600/dod-003/file_exists.yaml"

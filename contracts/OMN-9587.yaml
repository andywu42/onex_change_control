---
schema_version: "1.0.0"
ticket_id: OMN-9587
summary: "Replace runtime topic literals with canonical imports in event models (OMN-9152 follow-up)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnibase_core PR #892 CI checks pass (excluding deploy-gate which is expected to fail
      on feature PRs)"
    command: "gh pr checks 892 --repo OmniNode-ai/omnibase_core --json name,state"
  - kind: manual
    description: "10 event model files import from constants_event_types instead of hardcoding strings,
      as shown in PR #892 diff"
    command: "gh pr diff 892 --repo OmniNode-ai/omnibase_core"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "10 hardcoded topic literals replaced with canonical imports in event model files; local
      TOPIC_* symbol names preserved"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9587/dod-001/file_exists.yaml"
  - id: dod-002
    description: "omnibase_core PR #892 CI checks all pass (Mypy, contract compliance, cross-repo boundary,
      etc.)"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9587/dod-002/file_exists.yaml"

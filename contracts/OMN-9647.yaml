---
schema_version: "1.0.0"
ticket_id: OMN-9647
summary: "Validate runtime LLM endpoint env values against the canonical endpoint contract"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
  - protocols
evidence_requirements:
  - kind: tests
    description: "Endpoint contract, env guard, and model registry health-path tests passed locally."
    command: "uv run pytest tests/unit/contracts/test_llm_endpoints_contract.py tests/scripts/test_check_llm_endpoint_env_contract.py
      tests/unit/docker/test_model_registry_health_paths.py -q"
  - kind: manual
    description: "Read-only live probe proved the canonical running embedding endpoint is reachable and
      the current live env points at the disabled slot."
    command: "python scripts/check_llm_endpoint_env_contract.py --env-file <live .201 env extract>"
  - kind: ci
    description: "omnibase_infra PR #1398 carries CI and deploy-gate evidence for OMN-9647."
    command: "gh pr checks 1398 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Focused endpoint contract and guard test slice passed locally."
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9647/dod-001/file_exists.yaml"
  - id: dod-002
    description: "Endpoint env guard accepts the canonical running embedding endpoint."
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9647/dod-002/file_exists.yaml"
  - id: dod-003
    description: "Read-only live env validation rejects the current disabled .200 embedding URL."
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9647/dod-003/file_exists.yaml"
  - id: dod-004
    description: "PR #1398 opened with normal deploy-gate and receipt-gate enforcement."
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9647/dod-004/file_exists.yaml"

---
schema_version: "1.0.0"
ticket_id: OMN-9645
summary: "Remove model-only omnibase_infra packages from runtime entry points"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
  - protocols
evidence_requirements:
  - kind: tests
    description: "Focused generator and node tests prove runtime entry points match contract-backed node
      directories."
    command: "uv run pytest tests/scripts/test_generate_entry_points.py tests/integration/nodes/test_node_baseline_capture_effect.py
      tests/unit/nodes/node_baseline_capture/test_handler_baseline_capture.py -q"
  - kind: tests
    description: "Static generator and import checks prove all onex.nodes entry points have contracts
      and import cleanly."
    command: "uv run python scripts/generate_entry_points.py . --check && PYTHONPATH=src uv run python
      <entry-point import check>"
  - kind: ci
    description: "omnibase_infra PR #1395 carries CI and deploy-gate evidence for OMN-9645."
    command: "gh pr checks 1395 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Focused generator and baseline-capture test slice passed locally."
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9645/dod-001/file_exists.yaml"
  - id: dod-002
    description: "Entry-point sync and import checks passed with 91 contract-backed nodes, zero missing
      contracts, and zero import failures."
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9645/dod-002/file_exists.yaml"
  - id: dod-003
    description: "PR #1395 opened with normal deploy-gate and receipt-gate enforcement."
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9645/dod-003/file_exists.yaml"

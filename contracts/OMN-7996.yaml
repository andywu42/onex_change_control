---
schema_version: "1.0.0"
ticket_id: OMN-7996
summary: "fix(projection): pattern_enforcement_events — sentinel timestamp 2025-01-01, date filter corrected"
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 586 --repo OmniNode-ai/omnidash --json state,baseRefName -q '[.state,
          .baseRefName] | @tsv' | awk '$1 ~ /^(OPEN|MERGED)$/ && $2 == \"main\" {found=1} END {exit !found}'"
  - id: "dod-002"
    description: "PR targets main branch (verified at gate time)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 586 --repo OmniNode-ai/omnidash --json state,baseRefName -q '[.state,
          .baseRefName] | @tsv' | awk '$1 ~ /^(OPEN|MERGED)$/ && $2 == \"main\" {found=1} END {exit !found}'"
  - id: "dod-003"
    description: "Tests pass in CI"
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: ""
  - id: "dod-004"
    description: "Pre-commit config present"
    source: "generated"
    checks:
      - check_type: "file_exists"
        check_value: ".pre-commit-config.yaml"

---
schema_version: '1.0.0'
ticket_id: OMN-9547
summary: 'fix(OMN-9547): arm auto-merge explicitly as SQUASH to prevent silent queue drop'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnibase_core PR #887 updates .github/workflows/auto-merge.yml to pass --squash explicitly
      to gh pr merge --auto, eliminating the repository-default fallback that was silently arming PRs
      as MERGE and dropping them from the SQUASH-only merge queue."
    command: gh pr view 887 --repo OmniNode-ai/omnibase_core --json state -q .state
  - kind: ci
    description: "Auto-merge workflow change verified by inspecting the PR diff contents — only the gh
      pr merge invocation line is touched, adding --squash."
    command: gh pr diff 887 --repo OmniNode-ai/omnibase_core
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: "auto-merge.yml passes --squash explicitly to gh pr merge --auto, preventing silent MERGE
      fallback that breaks SQUASH-only merge queues"
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-9547/dod-001/file_exists.yaml
  - id: dod-002
    description: "omnibase_core PR #887 is OPEN and contains the auto-merge workflow fix"
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-9547/dod-002/file_exists.yaml
  - id: dod-003
    description: "OMN-9548 guard permits SQUASH and blocks only MERGE/REBASE, making the explicit --squash
      flag both required and permitted"
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-9547/dod-003/file_exists.yaml
  - id: dod-004
    description: "Empirical confirmation — omnimarket#388 was observed armed as MERGE at 20:39Z on 2026-04-23
      via github-actions bot, matching feedback_merge_queue_method_mismatch.md pattern"
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-9547/dod-004/file_exists.yaml

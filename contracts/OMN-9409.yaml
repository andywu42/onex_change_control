---
schema_version: '1.0.0'
ticket_id: OMN-9409
summary: 'chore(runtime): clean up stale ONEX_OUTPUT_TOPIC refs from source docs [OMN-9409]'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: docs
    description: docker/README.md and docs/testing/INTEGRATION_TESTING.md no longer advertise ONEX_OUTPUT_TOPIC
      / ONEX_INPUT_TOPIC as active
    command: git diff origin/main -- docker/README.md docs/testing/INTEGRATION_TESTING.md
  - kind: docs
    description: src/omnibase_infra/runtime/models/model_runtime_config.py docstring updated to mark ONEX_*_TOPIC
      as removed (OMN-8784)
    command: git diff origin/main -- src/omnibase_infra/runtime/models/model_runtime_config.py
  - kind: tests
    description: tests/unit/runtime/test_stale_topic_env_var_docs_removal.py guards against re-introducing
      ONEX_*_TOPIC env var in env-example files and integration test helpers
    command: uv run pytest tests/unit/runtime/test_stale_topic_env_var_docs_removal.py -v
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: Operator-facing docs (docker/README.md, docs/testing/INTEGRATION_TESTING.md) no longer
      show ONEX_*_TOPIC as an active override
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-9409/dod-001/file_exists.yaml
  - id: dod-002
    description: model_runtime_config.py docstring marks ONEX_INPUT_TOPIC / ONEX_OUTPUT_TOPIC as removed
      in OMN-8784
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-9409/dod-002/file_exists.yaml
  - id: dod-003
    description: Unit test guards env-example files and integration test helpers against re-introducing
      the banned env vars
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-9409/dod-003/file_exists.yaml

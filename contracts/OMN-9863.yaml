---
schema_version: "1.0.0"
ticket_id: OMN-9863
summary: "Add CLI entrypoint with git-diff parsing to scripts/ci/detect_test_paths.py — emits ModelTestSelection
  JSON for change-aware test selection (smart mode + main-branch full-suite mode)"
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: ci
    description: "omnibase_core PR #932 CI checks pass"
    command: "gh pr checks 932 --repo OmniNode-ai/omnibase_core --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "scripts/ci/detect_test_paths.py exposes a main() argparse CLI accepting --changed-files-from,
      --ref-name, --event-name, --adjacency, and --feature-flag, and is invokable via `python -m scripts.ci.detect_test_paths`"
    source: generated
    checks:
      - check_type: command
        check_value: "uv run python -m scripts.ci.detect_test_paths --help"
  - id: dod-002
    description: "CLI integration tests pass (test_cli_emits_valid_model_test_selection_json + test_cli_full_suite_on_main)
      — covers smart-mode JSON emission, main-branch full-suite reason, and ModelTestSelection schema
      conformance"
    source: generated
    checks:
      - check_type: test_passes
        check_value: "uv run pytest tests/unit/scripts/ci/test_detect_test_paths_cli.py -q"
  - id: dod-003
    description: "Full tests/unit/scripts/ci/ suite remains green after CLI addition (no regression in
      existing detect_test_paths test coverage)"
    source: generated
    checks:
      - check_type: test_passes
        check_value: "uv run pytest tests/unit/scripts/ci/ -q"

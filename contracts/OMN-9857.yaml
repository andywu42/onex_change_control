---
# ModelTicketContract — change-aware test selection (epic OMN-9826)
# Plan: docs/plans/change-aware-ci-omnibase-core.md
schema_version: "1.0.0"
ticket_id: OMN-9857
summary: "Rollout activation criteria for change-aware test selection (shadow phase, gate, rollback triggers)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnibase_core PR #924 CI checks pass"
    command: "gh pr checks 924 --repo OmniNode-ai/omnibase_core --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Rollout doc has the 3 required sections (Shadow phase, Activation gate, Rollback trigger)
      and enumerates exactly 4 numbered activation conditions"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -E '^## (Shadow phase|Activation gate|Rollback trigger)' scripts/ci/test_selection_rollout.md"

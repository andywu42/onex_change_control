---
schema_version: '1.0.0'
ticket_id: OMN-9151
summary: '[Epic] Purge hardcoded topics — contract-first enforcement (2026-04-18 sweep, OMN-5257/OMN-6931
  follow-up)'
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'Epic-level acceptance is the merge of all child tickets; evidence here is a pointer
      to child-ticket completion rather than a direct artifact.'
    command: 'gh pr checks'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: 'Child ticket OMN-9154 (CLI-layer DEFAULT_INTROSPECTION_TOPIC drift) closed via omnibase_infra#1392'
    source: generated
    checks:
      - check_type: command
        check_value: 'gh pr view 1392 --repo OmniNode-ai/omnibase_infra --json state -q .state'
  - id: dod-002
    description: 'Audit doc committed capturing the 2026-04-18 hardcoded-topics sweep findings'
    source: generated
    checks:
      - check_type: file_exists
        check_value: 'drift/dod_receipts/OMN-9151/dod-002/command.yaml'
  - id: dod-003
    description: 'Contract/schema artifact file exists'
    source: generated
    checks:
      - check_type: file_exists
        check_value: 'contracts/OMN-9151.yaml'

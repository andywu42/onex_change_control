---
schema_version: "1.0.0"
ticket_id: OMN-9534
summary: "github_webhook/contract.yaml fails 4 compliance checks — rename to integration_config.yaml and
  fix pre-commit exclude"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "onex compliance check shows github_webhook no longer fails"
    command: "uv run onex compliance check --repo-root . 2>&1 | grep github_webhook"
  - kind: manual
    description: "integration_config.yaml exists at canonical path"
    command: "ls src/omnibase_infra/contracts/integrations/github_webhook/integration_config.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "github_webhook no longer appears in compliance failures — file renamed to integration_config.yaml"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9534/dod-001/file_exists.yaml"
  - id: dod-002
    description: "integration_config.yaml exists at src/omnibase_infra/contracts/integrations/github_webhook/"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9534/dod-002/file_exists.yaml"

---
schema_version: "1.0.0"
ticket_id: OMN-9624
summary: "Extend EnumAgentCapability with TECH_DEBT_TRIAGE for A2A routing reducer"
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: tests
    description: "29/29 unit tests pass for EnumAgentCapability"
    command: "PYTHONPATH=src uv run pytest tests/unit/enums/test_enum_agent_capability.py -v"
  - kind: manual
    description: "mypy --strict and ruff clean on changed files"
    command: "uv run mypy src/omnibase_core/enums/enum_agent_capability.py --strict && uv run ruff check
      src/ tests/"
  - kind: ci
    description: "omnibase_core PR #897 CI green"
    command: "gh pr checks 897 --repo OmniNode-ai/omnibase_core --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "29/29 unit tests pass for EnumAgentCapability including TECH_DEBT_TRIAGE member"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9624/dod-001/file_exists.yaml"
  - id: dod-002
    description: "mypy --strict and ruff clean; all 54 pre-commit hooks pass"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9624/dod-002/file_exists.yaml"
  - id: dod-003
    description: "deploy-gate simulation passes for OMN-9624 without bypass token (enum-only, no service
      restart required)"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9624/dod-003/command.yaml"

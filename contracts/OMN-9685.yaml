---
schema_version: "1.0.0"
ticket_id: OMN-9685
summary: "Narrow deploy-gate validator scope and remove skip-token bypass"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "22 regression tests cover enum-only, model-only, protocol-only, runtime diff, skip-token
      ignored, real evidence pass paths"
    command: "uv run pytest tests/unit/scripts/validation/test_validate_pr_deploy_required.py -v"
  - kind: ci
    description: "omnibase_core PR #902 CI green"
    command: "gh pr checks 902 --repo OmniNode-ai/omnibase_core --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "22 regression tests pass, covering narrowed pattern matching and bypass removal"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9685/dod-001/file_exists.yaml"
  - id: dod-002
    description: "Validator rejects skip-deploy-gate tokens in PR body regardless of content"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9685/dod-002/file_exists.yaml"

---
schema_version: "1.0.0"
ticket_id: OMN-9557
summary: "Consolidate registration subscription ownership under runtime contract auto-wiring"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - protocols
  - topics
evidence_requirements:
  - kind: tests
    description: "Focused unit tests prove registration plugin no longer owns duplicate local event-bus
      subscription wiring."
    command: "uv run pytest tests/unit/runtime/auto_wiring/test_wiring_subscribe.py tests/unit/nodes/node_registration_orchestrator/test_plugin_wire_dispatchers_deferred.py
      -q"
  - kind: manual
    description: "Live runtime baseline shows duplicate heartbeat consumers that PR #1394 is intended
      to remove after deploy."
    command: "docker logs --since 10m omninode-runtime-effects | grep 'WIRING-CALLBACK] Message received
      on topic=onex.evt.platform.node-heartbeat.v1'"
  - kind: ci
    description: "omnibase_infra PR #1394 carries deploy-gate contract and stacked CI evidence for OMN-9557."
    command: "gh pr checks 1394 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Focused registration ownership unit test slice passed locally."
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9557/dod-001/file_exists.yaml"
  - id: dod-002
    description: "Live .201 baseline captured duplicate heartbeat subscription ownership before PR #1394
      deploy."
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9557/dod-002/file_exists.yaml"
  - id: dod-003
    description: "PR #1394 includes per-repo deploy evidence and remains stacked behind OMN-9550."
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9557/dod-003/file_exists.yaml"

---
schema_version: '1.0.0'
ticket_id: OMN-9457
summary: 'fix(runtime-effects): contain async-incompatible handlers that call asyncio.run() under async
  boot [OMN-9457]'
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: Deterministic containment added for handlers whose construction raises the CPython asyncio.run
      async-incompat RuntimeError signature; surfaced via ModelAutoWiringReport.quarantined_handlers with
      structured EnumQuarantineReason.ASYNC_INCOMPATIBLE.
    command: gh pr view 1380 --repo OmniNode-ai/omnibase_infra --json files
  - kind: tests
    description: Unit tests cover detector (CPython exact message, Runner.run variant, full __cause__/__context__
      traversal, self-referential chain), wire-level quarantine (normal/contained/wrapped/mixed/generic),
      and strict all_handlers_quarantined predicate (skip+quarantine mixed not collapsed).
    command: uv run pytest tests/unit/runtime/auto_wiring/test_handler_wiring_async_incompat_quarantine.py
      -v
  - kind: tests
    description: Integration test boots wire_from_manifest against a real importable module containing
      an async-incompat handler and asserts containment end-to-end.
    command: uv run pytest tests/integration/test_auto_wiring_async_incompat_quarantine_integration.py
      -v
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: PR opened against main branch for omnibase_infra (#1380)
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-9457/dod-001/file_exists.yaml
  - id: dod-002
    description: Contract artifact file present in onex_change_control
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-9457/dod-002/file_exists.yaml
  - id: dod-003
    description: Unit tests for detector + wire-level containment + strict all_handlers_quarantined predicate
      pass
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-9457/dod-003/file_exists.yaml
  - id: dod-004
    description: Integration test boots wire_from_manifest against a real importable module and asserts
      containment end-to-end
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-9457/dod-004/file_exists.yaml

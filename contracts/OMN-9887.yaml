---
# ModelTicketContract — runtime aliveness probe Wave 3 contracts
# Plan: docs/plans/2026-04-26-runtime-lifecycle-hardening-plan.md (Task 8)
schema_version: "1.0.0"
ticket_id: OMN-9887
summary: "ModelRuntimeAlivenessProbeCommand + ModelRuntimeAlivenessProbeReceipt + EnumProbeFailureState
  in omnibase_core (Wave 3 contracts for the runtime-aliveness probe runner shipped in Task 9)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnibase_core PR #945 for OMN-9887 CI checks pass (canonical implementation lives at
      omnibase_core/src/omnibase_core/models/runtime/model_runtime_aliveness_probe.py and model_runtime_aliveness_probe_receipt.py
      plus omnibase_core/src/omnibase_core/enums/enum_probe_failure_state.py)"
    command: "gh pr checks 945 --repo OmniNode-ai/omnibase_core --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "ModelRuntimeAlivenessProbeCommand: required-field enforcement and failure_states literal-typing
      via EnumProbeFailureState (2 unit tests test_probe_command_required_fields and test_probe_command_rejects_unknown_failure_state
      in tests/unit/models/runtime/test_model_runtime_aliveness_probe.py) — receipt at drift/dod_receipts/OMN-9887/dod-001/"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9887/dod-001/file_exists.yaml"
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9887/dod-001/file_exists.yaml &&
          grep -q '^evidence_item_id: dod-001$' drift/dod_receipts/OMN-9887/dod-001/file_exists.yaml &&
          grep -q '^ticket_id: OMN-9887$' drift/dod_receipts/OMN-9887/dod-001/file_exists.yaml && grep
          -q 'probe_command_required_fields' drift/dod_receipts/OMN-9887/dod-001/file_exists.yaml"
  - id: dod-002
    description: "Timeout configurability: ModelRuntimeAlivenessProbeCommand.from_env honors RUNTIME_ALIVENESS_TIMEOUT_SECONDS
      with the 30s default and falls back to default on invalid / non-positive values (2 unit tests test_timeout_default_is_30s_and_configurable_via_env
      and test_timeout_env_invalid_value_falls_back_to_default) — receipt at drift/dod_receipts/OMN-9887/dod-002/"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9887/dod-002/file_exists.yaml"
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9887/dod-002/file_exists.yaml &&
          grep -q '^evidence_item_id: dod-002$' drift/dod_receipts/OMN-9887/dod-002/file_exists.yaml &&
          grep -q '^ticket_id: OMN-9887$' drift/dod_receipts/OMN-9887/dod-002/file_exists.yaml && grep
          -q 'RUNTIME_ALIVENESS_TIMEOUT_SECONDS' drift/dod_receipts/OMN-9887/dod-002/file_exists.yaml"
  - id: dod-003
    description: "ModelRuntimeAlivenessProbeReceipt PASS/FAIL invariants: PASS receipts must have empty
      failure_states and FAIL receipts must list at least one (2 unit tests test_receipt_pass_requires_empty_failure_states
      and test_receipt_fail_requires_at_least_one_failure_state) — receipt at drift/dod_receipts/OMN-9887/dod-003/"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9887/dod-003/file_exists.yaml"
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9887/dod-003/file_exists.yaml &&
          grep -q '^evidence_item_id: dod-003$' drift/dod_receipts/OMN-9887/dod-003/file_exists.yaml &&
          grep -q '^ticket_id: OMN-9887$' drift/dod_receipts/OMN-9887/dod-003/file_exists.yaml && grep
          -q 'receipt_pass_requires_empty_failure_states' drift/dod_receipts/OMN-9887/dod-003/file_exists.yaml"

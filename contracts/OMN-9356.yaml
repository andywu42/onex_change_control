---
schema_version: '1.0.0'
ticket_id: OMN-9356
summary: 'feat(core): runtime_sha_match DoD gate — hard-block Done if deployed SHA != merge SHA'
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: ci
    description: CI passes on merge PR
    command: gh pr checks
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: PR opened against main branch in omnibase_core
    source: generated
    checks:
      - check_type: command
        check_value: 'gh pr view 872 --repo OmniNode-ai/omnibase_core --json state,baseRefName -q ''[.state,
          .baseRefName] | @tsv'''
  - id: dod-002
    description: Contract file present in onex_change_control
    source: generated
    checks:
      - check_type: command
        check_value: test -f contracts/OMN-9356.yaml
  - id: dod-003
    description: Receipt files present for all dod evidence items
    source: generated
    checks:
      - check_type: command
        check_value: test -f drift/dod_receipts/OMN-9356/dod-001/command.yaml && test -f drift/dod_receipts/OMN-9356/dod-002/command.yaml
          && test -f drift/dod_receipts/OMN-9356/dod-003/command.yaml

---
# ModelTicketContract — change-aware test selection (epic OMN-9826)
# Plan: docs/plans/change-aware-ci-omnibase-core.md
schema_version: "1.0.0"
ticket_id: OMN-9864
summary: "Adjacency map loader with shape validation (ModelAdjacencyMap, ModelAdjacencyEntry, load_adjacency_map)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnibase_core PR #928 CI checks pass"
    command: "gh pr checks 928 --repo OmniNode-ai/omnibase_core --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Loader unit tests pass — 3 tests covering repo YAML parse, unknown shared module rejection,
      and every src module having adjacency entry; ModelAdjacencyMap enforces shape consistency at load
      time — receipt at drift/dod_receipts/OMN-9864/dod-001/"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9864/dod-001/file_exists.yaml"
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9864/dod-001/file_exists.yaml &&
          grep -q '^evidence_item_id: dod-001$' drift/dod_receipts/OMN-9864/dod-001/file_exists.yaml &&
          grep -q '^ticket_id: OMN-9864$' drift/dod_receipts/OMN-9864/dod-001/file_exists.yaml && grep
          -q '^exit_code: 0$' drift/dod_receipts/OMN-9864/dod-001/file_exists.yaml"

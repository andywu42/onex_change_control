---
schema_version: "1.0.0"
ticket_id: OMN-9504
summary: "Polymorphic agent scrub — remove all active references, replace with general-purpose, fail-fast
  routing"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: All 3 PRs across omniclaude, omnibase_core, omnibase_infra merged to main
    command: gh pr view 1391 --repo OmniNode-ai/omniclaude --json mergedAt -q .mergedAt
  - kind: tests
    description: Pre-commit hooks pass (ruff, mypy, pyright, bandit) on all affected repos
    command: uv run pre-commit run --all-files
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001-omniclaude-1391
    description: "omniclaude#1391 merged to main — remove polymorphic-agent, replace with general-purpose,
      fail-fast routing (57 files, behavioral cutover)"
    source: generated
    checks:
      - check_type: command
        check_value: "gh pr view 1391 --repo OmniNode-ai/omniclaude --json mergedAt -q .mergedAt"
  - id: dod-002-omnibase_core-881
    description: "omnibase_core#881 merged to main — remove polymorphic-agent references from models and
      tests"
    source: generated
    checks:
      - check_type: command
        check_value: "gh pr view 881 --repo OmniNode-ai/omnibase_core --json mergedAt -q .mergedAt"
  - id: dod-003-omnibase_infra-1382
    description: "omnibase_infra#1382 merged to main — remove polymorphic-agent references from models,
      tests, and migrations"
    source: generated
    checks:
      - check_type: command
        check_value: "gh pr view 1382 --repo OmniNode-ai/omnibase_infra --json mergedAt -q .mergedAt"
status: "In Progress"
repo: "multi"
pr_number: 0

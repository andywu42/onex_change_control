---
schema_version: '1.0.0'
ticket_id: OMN-9458
summary: 'fix(ci): hard-fail reusable runtime boot on degraded runtime-effects or auto-wiring errors [OMN-9458]'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: PR 1379 in omnibase_infra strengthens reusable-runtime-boot.yml with effects health gate,
      startup log scan, and degraded-state detection
    command: gh pr view 1379 --repo OmniNode-ai/omnibase_infra --json state -q .state
  - kind: tests
    description: Schema test suite for reusable-runtime-boot.yml passes (22 tests covering effects health
      gate, startup log patterns, degraded-vs-never-responded distinction)
    command: uv run pytest tests/ci/test_reusable_runtime_boot_schema.py -v
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: Runtime boot workflow asserts runtime-effects becomes healthy
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-9458/dod-001/file_exists.yaml
  - id: dod-002
    description: Runtime boot workflow fails on duplicate dispatcher registration during startup
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-9458/dod-002/file_exists.yaml
  - id: dod-003
    description: Runtime boot workflow fails on auto-wiring startup failures
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-9458/dod-003/file_exists.yaml
  - id: dod-004
    description: Failure artifacts remain available for diagnosis (smoke-result.json includes effects_health
      and startup_logs)
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-9458/dod-004/file_exists.yaml
  - id: dod-005
    description: CI blocks merge when runtime starts in degraded state (distinguishes degraded from never-responded,
      surfaces payload in error message)
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-9458/dod-005/file_exists.yaml

---
schema_version: '1.0.0'
ticket_id: OMN-9277
summary: 'Epic — require behavior_proven DoD evidence for handler/pipeline/consumer/effect tickets'
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: ci
    description: CI passes on the merge PR for each child ticket
    command: gh pr checks
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: 'Epic tracked via child ticket OMN-9278 (Wave 2 children 9279/9280/9281 pending — contracts
      seeded in their own PRs)'
    source: generated
    checks:
      - check_type: command
        check_value: 'test -f contracts/OMN-9278.yaml'

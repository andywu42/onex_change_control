---
schema_version: "1.0.0"
ticket_id: OMN-9646
summary: "Replace aiokafka group describe path that crashes on Redpanda metadata"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - protocols
  - public_api
evidence_requirements:
  - kind: tests
    description: "Focused runtime health monitor tests prove active, empty, and failure group-state paths."
    command: "uv run pytest tests/unit/services/test_service_runtime_health_monitor.py -q"
  - kind: manual
    description: "Read-only .201 probe proves confluent-kafka can list runtime consumer group states without
      the aiokafka UnicodeDecodeError."
    command: "docker exec -i omninode-runtime python - <<'PY' ... AdminClient.list_consumer_groups(...)"
  - kind: ci
    description: "omnibase_infra PR #1397 carries CI and deploy-gate evidence for OMN-9646."
    command: "gh pr checks 1397 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Focused runtime health monitor unit test slice passed locally."
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9646/dod-001/file_exists.yaml"
  - id: dod-002
    description: "Live .201 confluent-kafka probe returned usable group states without decode failure."
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9646/dod-002/file_exists.yaml"
  - id: dod-003
    description: "PR #1397 opened with normal deploy-gate and receipt-gate enforcement."
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9646/dod-003/file_exists.yaml"

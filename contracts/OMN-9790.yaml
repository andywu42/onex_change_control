---
schema_version: "1.0.0"
ticket_id: OMN-9790
summary: "Wave C Task 10 — migration script to backfill legacy receipts with verifier/probe_command/probe_stdout/schema_version
  sentinels and ADVISORY status, preserving original_status"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnibase_core PR #942 CI checks pass"
    command: "gh pr checks 942 --repo OmniNode-ai/omnibase_core --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "onex_change_control/scripts/migrate_receipts_to_advisory.py exists with migrate_receipt_file(path)
      supporting both YAML and JSON suffixes and a --dry-run flag"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9790/dod-001/command.yaml && grep
          -q '^evidence_item_id: dod-001$' drift/dod_receipts/OMN-9790/dod-001/command.yaml && grep -q
          '^ticket_id: OMN-9790$' drift/dod_receipts/OMN-9790/dod-001/command.yaml"
  - id: dod-002
    description: "4 unit tests pass for migrate_receipts (legacy-receipt-without-verifier marked ADVISORY,
      idempotent re-run produces identical bytes, preserves original_status, already-advisory receipts
      not modified)"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9790/dod-002/command.yaml && grep
          -q '^evidence_item_id: dod-002$' drift/dod_receipts/OMN-9790/dod-002/command.yaml && grep -q
          '^ticket_id: OMN-9790$' drift/dod_receipts/OMN-9790/dod-002/command.yaml"
  - id: dod-003
    description: "Dry-run report saved to docs/tracking/2026-04-26-receipt-migration-dryrun.txt enumerating
      receipts under drift/dod_receipts/ and .evidence/ that would be backfilled"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9790/dod-003/command.yaml && grep
          -q '^evidence_item_id: dod-003$' drift/dod_receipts/OMN-9790/dod-003/command.yaml && grep -q
          '^ticket_id: OMN-9790$' drift/dod_receipts/OMN-9790/dod-003/command.yaml"
  - id: dod-004
    description: "Real-data idempotency verified: a second invocation of migrate_receipts_to_advisory
      after the first migration produces zero modifications"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9790/dod-004/command.yaml && grep
          -q '^evidence_item_id: dod-004$' drift/dod_receipts/OMN-9790/dod-004/command.yaml && grep -q
          '^ticket_id: OMN-9790$' drift/dod_receipts/OMN-9790/dod-004/command.yaml"

---
schema_version: "1.0.0"
ticket_id: OMN-9550
summary: "Defer auto-wired runtime subscriptions until after dispatch-engine freeze"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - protocols
  - public_api
evidence_requirements:
  - kind: tests
    description: "Focused unit and integration tests prove subscriptions are deferred until explicit post-freeze
      commit."
    command: "uv run pytest tests/unit/runtime/auto_wiring/test_wiring_subscribe.py tests/unit/runtime/test_kernel.py::TestBootstrap::test_bootstrap_defers_auto_wiring_subscriptions_until_after_freeze
      tests/integration/runtime/test_auto_wiring_deferred_subscription_order.py -q"
  - kind: ci
    description: "omnibase_infra PR #1388 carries deploy-gate contract and CI evidence for OMN-9550."
    command: "gh pr checks 1388 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Focused deferred-subscription unit and integration test slice passed locally."
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9550/dod-001/file_exists.yaml"
  - id: dod-002
    description: "PR #1388 includes per-repo deploy evidence and remains gated by normal CI/receipt checks."
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9550/dod-002/file_exists.yaml"

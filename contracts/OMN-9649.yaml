---
schema_version: "1.0.0"
ticket_id: OMN-9649
summary: "Publish NO_DISPATCHER runtime messages to the derived DLQ before offset commit"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - protocols
  - public_api
evidence_requirements:
  - kind: tests
    description: "Focused wiring, topic constants, extractor, and DLQ manifest tests passed locally."
    command: "uv run pytest tests/unit/runtime/test_event_bus_subcontract_wiring.py tests/unit/event_bus/test_topic_constants.py
      tests/unit/tools/test_contract_topic_extractor_yaml.py tests/unit/tools/test_contract_topic_extractor.py
      tests/integration/runtime/test_dlq_topic_manifest_coverage.py -q"
  - kind: tests
    description: "Kafka DLQ unit/integration-adjacent tests passed locally; broker-backed tests skipped
      because Kafka integration env was not enabled."
    command: "uv run pytest tests/unit/event_bus/test_kafka_event_bus.py tests/integration/event_bus/test_dlq_integration.py
      -q"
  - kind: tests
    description: "Topic enum generator and TopicProvisioner-adjacent coverage passed locally."
    command: "uv run pytest tests/unit/tools/test_topic_enum_generator.py tests/unit/tools/test_topic_constants_enum_coverage.py
      tests/unit/event_bus/test_service_topic_manager.py tests/integration/event_bus/test_kafka_bootstrap_no_localhost_fallback_integration.py
      -q"
  - kind: ci
    description: "omnibase_infra PR #1401 carries CI and deploy-gate evidence for OMN-9649."
    command: "gh pr checks 1401 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Focused runtime wiring, topic constants, extractor, and manifest test slice passed locally."
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9649/dod-001/file_exists.yaml"
  - id: dod-002
    description: "Kafka DLQ tests passed locally with broker-backed integration cases skipped by missing
      Kafka test env."
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9649/dod-002/file_exists.yaml"
  - id: dod-003
    description: "Topic enum and provisioning-adjacent tests passed locally."
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9649/dod-003/file_exists.yaml"
  - id: dod-004
    description: "PR #1401 opened with normal deploy-gate and receipt-gate enforcement."
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9649/dod-004/file_exists.yaml"

---
schema_version: "1.0.0"
ticket_id: OMN-9455
summary: "fix(deploy-agent): runtime scope must not recreate core infra (OMN-9455)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: Unit tests pin --no-deps on runtime compose-up and prove core infra services never appear
      in runtime commands
    command: "uv run pytest scripts/deploy-agent/tests/unit/test_executor_runtime_scope.py -v"
  - kind: ci
    description: Full deploy-agent suite passes (65 tests)
    command: "uv run pytest scripts/deploy-agent/tests/ -v"
  - kind: docs
    description: Runtime runbook documents recovery procedure with consistent phoenix handling in stop/up
      commands
    command: "ls docs/runbooks/emergency-runtime-refresh.md"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: Unit tests pass — runtime scope uses --no-deps and explicit service list, never includes
      core infra services
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9455/dod-001/command.yaml"
  - id: dod-002
    description: Full deploy-agent test suite passes (65/65)
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9455/dod-002/command.yaml"
  - id: dod-003
    description: Emergency runbook exists with consistent phoenix handling in stop/up commands
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9455/dod-003/command.yaml"

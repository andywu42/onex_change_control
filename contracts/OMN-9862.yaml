---
# ModelTicketContract — change-aware test selection (epic OMN-9826)
# Plan: docs/plans/change-aware-ci-omnibase-core.md
schema_version: "1.0.0"
ticket_id: OMN-9862
summary: "Add Pydantic output models for change-aware test selection (ModelTestSelection, EnumFullSuiteReason)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnibase_core PR #927 CI checks pass"
    command: "gh pr checks 927 --repo OmniNode-ai/omnibase_core --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Unit tests for ModelTestSelection / EnumFullSuiteReason pass — 3 tests covering full-suite
      reason validation, smart-mode reason rejection, matrix-length consistency"
    source: generated
    checks:
      - check_type: test_passes
        check_value: "uv run pytest tests/unit/scripts/ci/test_test_selection_models.py -v --no-header"

---
schema_version: "1.0.0"
ticket_id: OMN-9788
summary: "Harden receipt_gate.py adversarial invariants — enforce verifier ≠ runner, require non-empty
  probe_command + probe_stdout for executable check_types, downgrade ADVISORY/PENDING/sole-file_exists
  to fail-closed in the canonical CI gate at omnibase_core/src/omnibase_core/validation/receipt_gate.py"
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: ci
    description: "omnibase_core PR #934 for OMN-9788 CI checks pass (canonical receipt_gate hardening
      lives in omnibase_core)"
    command: "gh pr checks 934 --repo OmniNode-ai/omnibase_core --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "verifier ≠ runner enforcement: receipt_gate.py rejects receipts where verifier == runner
      (including whitespace-equivalent) and where verifier field is missing entirely (4 unit tests in
      TestReceiptGateAdversarialVerifier) — receipt at drift/dod_receipts/OMN-9788/dod-001/"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9788/dod-001/file_exists.yaml"
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9788/dod-001/file_exists.yaml &&
          grep -q '^evidence_item_id: dod-001$' drift/dod_receipts/OMN-9788/dod-001/file_exists.yaml &&
          grep -q '^ticket_id: OMN-9788$' drift/dod_receipts/OMN-9788/dod-001/file_exists.yaml"
  - id: dod-002
    description: "probe-field hardening: receipts missing probe_command, missing probe_stdout, with empty
      probe_stdout for executable check_types, or with whitespace-only probe_command are rejected (4 unit
      tests in TestReceiptGateAdversarialProbeFields) — receipt at drift/dod_receipts/OMN-9788/dod-002/"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9788/dod-002/file_exists.yaml"
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9788/dod-002/file_exists.yaml &&
          grep -q '^evidence_item_id: dod-002$' drift/dod_receipts/OMN-9788/dod-002/file_exists.yaml &&
          grep -q 'probe_command' drift/dod_receipts/OMN-9788/dod-002/file_exists.yaml"
  - id: dod-003
    description: "ADVISORY/PENDING/sole-file_exists downgrade: explicit ADVISORY status fails, PENDING
      status fails, and a file_exists check with PASS status is downgraded to ADVISORY and fails (3 unit
      tests in TestReceiptGateAdvisoryDowngrade) — receipt at drift/dod_receipts/OMN-9788/dod-003/"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9788/dod-003/file_exists.yaml"
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9788/dod-003/file_exists.yaml &&
          grep -q '^evidence_item_id: dod-003$' drift/dod_receipts/OMN-9788/dod-003/file_exists.yaml &&
          grep -q 'ADVISORY' drift/dod_receipts/OMN-9788/dod-003/file_exists.yaml"
  - id: dod-004
    description: "Existing receipt_gate test suites still pass with the hardened gate (regression guard
      for downstream consumers in onex_change_control and CI receipt-gate workflow) — receipt at drift/dod_receipts/OMN-9788/dod-004/"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9788/dod-004/file_exists.yaml"
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9788/dod-004/file_exists.yaml &&
          grep -q '^evidence_item_id: dod-004$' drift/dod_receipts/OMN-9788/dod-004/file_exists.yaml &&
          grep -q '^exit_code: 0$' drift/dod_receipts/OMN-9788/dod-004/file_exists.yaml"

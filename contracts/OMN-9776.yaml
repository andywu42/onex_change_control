---
schema_version: "1.0.0"
ticket_id: OMN-9776
summary: "Wire backend_overrides['event_bus']='kafka' through RuntimeLocal via entry-point dispatch"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnibase_core PR #919 CI checks pass"
    command: "gh pr checks 919 --repo OmniNode-ai/omnibase_core --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "_create_event_bus + _create_kafka_event_bus added to runtime_local.py with default-path
      behavior preserved"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9776/dod-001/file_exists.yaml"
  - id: dod-002
    description: "tests/unit/runtime/test_runtime_local_event_bus_override.py exists with 12 unit tests
      covering default + kafka entry-point dispatch + failure modes"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9776/dod-002/file_exists.yaml"
  - id: dod-003
    description: "kafka_bootstrap added to KNOWN_BACKEND_KEYS so parse_backend_overrides accepts the new
      key"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9776/dod-003/file_exists.yaml"

---
schema_version: "1.0.0"
ticket_id: OMN-7537
summary: "Phase 1: Foundation — repo setup, metadata schema, onex pack/install CLI, 3 pilo"
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "omnibase_core PR #883 OPEN targeting main"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: 'gh pr view 883 --repo OmniNode-ai/omnibase_core --json state,baseRefName -q ''.state
          + ":" + .baseRefName'' | grep -qx OPEN:main'
  - id: "dod-002"
    description: "omnibase_core PR #883 diff includes cli_pack.py (pack CLI handler)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: 'gh pr view 883 --repo OmniNode-ai/omnibase_core --json files -q ''[.files[] | select(.path
          == "src/omnibase_core/cli/cli_pack.py")] | length > 0'' | grep -qx true'
  - id: "dod-003"
    description: "omnibase_core PR #883 diff includes cli_install.py (install CLI handler)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: 'gh pr view 883 --repo OmniNode-ai/omnibase_core --json files -q ''[.files[] | select(.path
          == "src/omnibase_core/cli/cli_install.py")] | length > 0'' | grep -qx true'
  - id: "dod-004"
    description: "omnibase_core PR #883 adds tests/unit/cli/test_cli_install.py with path-traversal +
      metadata coverage"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: 'gh pr view 883 --repo OmniNode-ai/omnibase_core --json files -q ''[.files[] | select(.path
          == "tests/unit/cli/test_cli_install.py")] | length > 0'' | grep -qx true'
  - id: "dod-005"
    description: "Contract/schema artifact file exists in onex_change_control"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "test -f contracts/OMN-7537.yaml"

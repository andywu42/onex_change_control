---
schema_version: "1.0.0"
ticket_id: OMN-9854
summary: "Verification checkpoint — confirm tests-gate aggregates dynamic matrix result without hardcoded
  40 split count, after Tasks 8-11 split-count refactor"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnibase_core PR #940 CI checks pass"
    command: "gh pr checks 940 --repo OmniNode-ai/omnibase_core --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "tests-gate and ci-summary job bodies contain no hardcoded 40 split count (matrix declarations
      and the force_full step are allowed exceptions per acceptance criteria)"
    source: generated
    checks:
      - check_type: command
        check_value: "bash -c \"! awk '/^  tests-gate:|^  ci-summary:/,/^  [a-z]/' .github/workflows/ci.yml
          | grep -nE '\\\\b40\\\\b'\""
  - id: dod-002
    description: "Branch-protection check names Quality Gate, Tests Gate, and CI Summary remain unchanged
      in ci.yml"
    source: generated
    checks:
      - check_type: command
        check_value: "bash -c \"grep -q 'name: Quality Gate' .github/workflows/ci.yml && grep -q 'name:
          Tests Gate' .github/workflows/ci.yml && grep -q 'name: CI Summary' .github/workflows/ci.yml\""
  - id: dod-003
    description: "ci.yml is valid YAML and passes actionlint + pre-commit workflow validation"
    source: generated
    checks:
      - check_type: command
        check_value: "uv run python -c \"import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))\""

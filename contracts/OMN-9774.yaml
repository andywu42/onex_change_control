---
schema_version: "1.0.0"
ticket_id: OMN-9774
summary: "build_loop projection consumer chain + build_loop_runs table + Kafka→Postgres E2E"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "6 unit tests for HandlerBuildLoopProjection pure-logic projection pass"
    command: "uv run pytest tests/unit/nodes/node_build_loop_projection_compute/ -v"
  - kind: tests
    description: "3 OMN-9126 auto-wiring boot tests pass against real contract.yaml manifests"
    command: "uv run pytest tests/integration/runtime/test_build_loop_auto_wiring_boot.py -v"
  - kind: ci
    description: "omnibase_infra PR #1421 CI checks pass"
    command: "gh pr checks 1421 --repo OmniNode-ai/omnibase_infra --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "6 unit tests for HandlerBuildLoopProjection (canonical extraction, envelope unwrap,
      fallback semantics, three failure modes) pass locally"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9774/dod-001/file_exists.yaml"
  - id: dod-002
    description: "OMN-9126 first-class CI gate green: real-manifest discover + wire_from_manifest, zero
      failures on both new contracts (node_build_loop_projection_compute + node_build_loop_write_effect)"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9774/dod-002/file_exists.yaml"
  - id: dod-003
    description: "pre-commit hooks pass (ONEX Pattern Validation, Topic Enum Drift, Topic Naming Lint,
      Architecture Validation, Handler Contract Compliance); mypy --strict clean on 20 new source files"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9774/dod-003/file_exists.yaml"

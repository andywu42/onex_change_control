---
schema_version: '1.0.0'
ticket_id: OMN-9425
summary: 'ci(deps): cascade omnibase_core releases to onex_change_control [OMN-9425]'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: CI passes on omnibase_core PR  #879
    command: gh pr checks 879 --repo OmniNode-ai/omnibase_core
  - kind: tests
    description: Unit test locks in the cascade-matrix mapping
    command: uv run pytest tests/scripts/test_dependency_cascade_matrix.py -v
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: PR opened against main branch
    source: generated
    checks:
      - check_type: command
        check_value: 'state=$(gh pr view {pr} --repo {repo} --json state,baseRefName -q ''[.state, .baseRefName]
          | @tsv''); [ "$(echo "$state" | cut -f1)" = "OPEN" ] && [ "$(echo "$state" | cut -f2)" = "main"
          ]'
  - id: dod-002
    description: Contract artifact file present in repo
    source: generated
    checks:
      - check_type: command
        check_value: test -f contracts/OMN-9425.yaml
  - id: dod-003
    description: CI passes on PR
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view {pr} --repo {repo} --json state -q .state

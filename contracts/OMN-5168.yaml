---
schema_version: "1.0.0"
ticket_id: OMN-5168
summary: "Add dod_evidence[] schema to ModelTicketContract"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: tests
    description: >
      25 new tests covering ModelDodCheck and ModelDodEvidenceItem validation, status lifecycle, source
      types, check types, YAML round-trip, and immutability all pass.
    command: >
      cd /Volumes/PRO-G40/Code/omni_worktrees/OMN-5168/onex_change_control && uv run pytest tests/test_dod_evidence_schema.py
      -v
  - kind: ci
    description: >
      mypy --strict passes on model_ticket_contract.py with new dod_evidence fields and no type errors.
    command: >
      cd /Volumes/PRO-G40/Code/omni_worktrees/OMN-5168/onex_change_control && uv run mypy src/onex_change_control/models/model_ticket_contract.py
      --strict
# golden_path: null — OMN-5168 is a schema definition ticket (public_api only, no topics/events flow)
dod_evidence:
  - id: "dod-001"
    description: "25 new tests covering DoD schema models pass"
    source: "manual"
    linear_dod_text: "25 tests covering ModelDodCheck and ModelDodEvidenceItem validation, status lifecycle,
      source types"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/test_dod_evidence_schema.py -v"
  - id: "dod-002"
    description: "mypy strict passes on ticket contract model"
    source: "manual"
    linear_dod_text: "mypy --strict passes on model_ticket_contract.py with new dod_evidence fields"
    checks:
      - check_type: "command"
        check_value: "uv run mypy src/onex_change_control/models/model_ticket_contract.py --strict"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""

---
schema_version: "1.0.0"
ticket_id: OMN-9393
summary: "fix(deploy-agent): unsigned trigger envelopes silently dropped by auth.py — implementation in
  OMN-9411"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - protocols
evidence_requirements:
  - kind: ci
    description: omnibase_infra PR 1367 (OMN-9411 implementation) merged to main
    command: gh pr view 1367 --repo OmniNode-ai/omnibase_infra --json state -q .state
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: omnibase_infra PR 1367 (signed trigger helper) merged to main
    checks:
      - check_type: command
        check_value: "gh pr view 1367 --repo OmniNode-ai/omnibase_infra --json state -q .state"

---
schema_version: "1.0.0"
ticket_id: OMN-9592
summary: "Implement ONCP lazy entry-point loading for metadata-scoped dependencies"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: tests
    description: "Focused discovery, packaged contract lookup, and ONCP install tests pass"
    command: "PYTHONPATH=src uv run pytest tests/unit/discovery/test_discovery_external_nodes.py tests/unit/cli/test_cli_node.py
      tests/unit/cli/test_cli_install.py -q"
  - kind: manual
    description: "Touched Python surfaces pass ruff and mypy"
    command: "PYTHONPATH=src uv run ruff check ... && PYTHONPATH=src uv run mypy ..."
  - kind: ci
    description: "omnibase_core PR #891 deploy-gate and contract-validation pass"
    command: "gh pr checks 891 --repo OmniNode-ai/omnibase_core --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Focused unit tests cover lazy discovery, packaged contract lookup, and ONCP install
      validation"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9592/dod-001/command.yaml"
  - id: dod-002
    description: "Touched Python surfaces pass lint, format, and type checks"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9592/dod-002/command.yaml"
  - id: dod-003
    description: "Ticket contract validates and deploy-gate simulation passes without bypass"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9592/dod-003/command.yaml"

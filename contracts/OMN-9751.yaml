---
schema_version: "1.0.0"
ticket_id: OMN-9751
summary: "Typed ModelFeatureFlagContract + loader for runtime and event_bus YAML contracts"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "10 unit tests for ModelFeatureFlagContract and loader pass"
    command: "uv run pytest tests/unit/models/contracts/test_model_service_contract.py -v"
  - kind: ci
    description: "omnibase_infra PR #1417 CI checks pass"
    command: "gh pr checks 1417 --repo OmniNode-ai/omnibase_infra --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "10 unit tests for ModelFeatureFlagContract round-trip, immutability, defaults, and loader
      pass"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9751/dod-001/file_exists.yaml"
  - id: dod-002
    description: "pre-commit hooks pass; mypy clean; CI green on omnibase_infra PR #1417"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9751/dod-002/file_exists.yaml"

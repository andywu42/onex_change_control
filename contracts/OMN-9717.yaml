---
schema_version: "1.0.0"
ticket_id: OMN-9717
summary: "Bust PR_EVENT_BODY cache on body update — receipt-gate fetches live PR body via gh api"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnibase_core PR #906 CI checks pass (receipt-gate reads live body)"
    command: "gh pr checks 906 --repo OmniNode-ai/omnibase_core --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "receipt-gate.yml updated to fetch live PR body via gh pr view instead of frozen event
      payload"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9717/dod-001/file_exists.yaml"
  - id: dod-002
    description: "CI passes on omnibase_core PR #906 with receipt-gate reading OMN-9717 from live body"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9717/dod-002/file_exists.yaml"

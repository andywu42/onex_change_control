---
schema_version: "1.0.0"
ticket_id: OMN-9631
summary: "feat(infra): migration 069 — create remote_task_state table for A2A delegation bridge"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "16 unit tests for migration 069 pass"
    command: "uv run pytest tests/unit/db/test_migration_069.py -v"
  - kind: ci
    description: "omnibase_infra PR #1409 CI checks pass"
    command: "gh pr checks 1409 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Requirements implemented — forward migration 069_create_remote_task_state.sql + rollback_069_drop_remote_task_state.sql
      added; schema_fingerprint.sha256 re-stamped for 55 migrations"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9631/dod-001/file_exists.yaml"
  - id: dod-002
    description: "Tests added — 16 unit tests in tests/unit/db/test_migration_069.py covering schema shape,
      CHECK constraints, indexes, sentinel, and rollback; all passing"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9631/dod-002/file_exists.yaml"
  - id: dod-003
    description: "Code reviewed — CodeRabbit reviewed PR #1409; gate/CodeRabbit Thread Check passed (no
      unresolved threads)"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9631/dod-003/file_exists.yaml"
  - id: dod-004
    description: "Documentation: internal DB migration only — no public API surface, no external docs
      impacted. schema_fingerprint.sha256 updated as the migration's self-documenting artifact."
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9631/dod-004/file_exists.yaml"

---
schema_version: "1.0.0"
ticket_id: OMN-597
summary: "[BETA-03] Implement fsm_metadata ↔ metadata dict consistency"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "omnibase_core PR #885 OPEN targeting main"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: 'gh pr view 885 --repo OmniNode-ai/omnibase_core --json state,baseRefName -q ''.state
          + ":" + .baseRefName'' | grep -qx OPEN:main'
  - id: "dod-002"
    description: "omnibase_core PR #885 diff includes model_reducer_output.py (typed fsm_metadata field)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: 'gh pr view 885 --repo OmniNode-ai/omnibase_core --json files -q ''[.files[] | select(.path
          == "src/omnibase_core/models/reducer/model_reducer_output.py")] | length > 0'' | grep -qx true'
  - id: "dod-003"
    description: "omnibase_core PR #885 diff includes node_reducer.py (process_fsm rewired to derive extras
      from typed model)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: 'gh pr view 885 --repo OmniNode-ai/omnibase_core --json files -q ''[.files[] | select(.path
          == "src/omnibase_core/nodes/node_reducer.py")] | length > 0'' | grep -qx true'
  - id: "dod-004"
    description: "omnibase_core PR #885 adds tests/unit/nodes/test_node_reducer_fsm_metadata_consistency.py
      (12 round-trip consistency tests)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: 'gh pr view 885 --repo OmniNode-ai/omnibase_core --json files -q ''[.files[] | select(.path
          == "tests/unit/nodes/test_node_reducer_fsm_metadata_consistency.py")] | length > 0'' | grep
          -qx true'

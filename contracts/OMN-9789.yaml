---
schema_version: "1.0.0"
ticket_id: OMN-9789
summary: "Wave B Task 9 — add re-probe verification mode to canonical receipt-gate (verify_receipt_by_reexecuting_probe
  + allowlist + --reexecute-probes CLI flag)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnibase_core PR #943 CI checks pass"
    command: "gh pr checks 943 --repo OmniNode-ai/omnibase_core --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "verify_receipt_by_reexecuting_probe added to omnibase_core/validation/receipt_gate.py
      with PROBE_COMMAND_ALLOWLIST containing exactly the 10 entries (gh pr view, gh pr checks, gh api,
      psql, curl, uv run pytest, test -f, test -d, true, false)"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9789/dod-001/command.yaml && grep
          -q '^evidence_item_id: dod-001$' drift/dod_receipts/OMN-9789/dod-001/command.yaml && grep -q
          '^ticket_id: OMN-9789$' drift/dod_receipts/OMN-9789/dod-001/command.yaml"
  - id: dod-002
    description: "3 unit tests pass for verify_receipt_by_reexecuting_probe (passing probe returns PASS,
      failing probe returns FAIL with exit_code, allowlist rejection of arbitrary commands)"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9789/dod-002/command.yaml && grep
          -q '^evidence_item_id: dod-002$' drift/dod_receipts/OMN-9789/dod-002/command.yaml && grep -q
          '^ticket_id: OMN-9789$' drift/dod_receipts/OMN-9789/dod-002/command.yaml"
  - id: dod-003
    description: "--reexecute-probes CLI flag wired into onex_change_control/scripts/check_dod_compliance.py
      with shell=False (shlex.split) and 120s timeout cap; default OFF in CI"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9789/dod-003/command.yaml && grep
          -q '^evidence_item_id: dod-003$' drift/dod_receipts/OMN-9789/dod-003/command.yaml && grep -q
          '^ticket_id: OMN-9789$' drift/dod_receipts/OMN-9789/dod-003/command.yaml"
